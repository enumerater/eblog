<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enumerate.notification.mapper.NotificationMapper">

    <resultMap id="notificationResultMap" type="com.enumerate.notification.entity.Notification">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="type" column="type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="relatedId" column="related_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="notificationResultMap">
        SELECT id, user_id, type, title, content, related_id, is_read, created_at
        FROM notifications
        WHERE user_id = #{userId} OR user_id = 0
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM notifications
        WHERE user_id = #{userId} OR user_id = 0
    </select>

    <select id="countUnreadByUserId" resultType="long">
        SELECT COUNT(*) FROM notifications
        WHERE (user_id = #{userId} OR user_id = 0) AND is_read = 0
    </select>

    <select id="findById" resultMap="notificationResultMap">
        SELECT id, user_id, type, title, content, related_id, is_read, created_at
        FROM notifications WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, type, title, content, related_id, is_read, created_at)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{relatedId}, 0, NOW())
    </insert>

    <update id="markAsRead">
        UPDATE notifications SET is_read = 1
        WHERE id = #{id} AND (user_id = #{userId} OR user_id = 0)
    </update>

    <update id="markAllAsRead">
        UPDATE notifications SET is_read = 1
        WHERE (user_id = #{userId} OR user_id = 0) AND is_read = 0
    </update>

</mapper>