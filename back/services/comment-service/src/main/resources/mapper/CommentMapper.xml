<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enumerate.comment.mapper.CommentMapper">

    <resultMap id="commentResultMap" type="com.enumerate.comment.entity.Comment">
        <id property="id" column="id"/>
        <result property="articleId" column="article_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="author" column="author"/>
        <result property="content" column="content"/>
        <result property="userId" column="user_id"/>
        <result property="status" column="status"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findById" resultMap="commentResultMap">
        SELECT * FROM comments WHERE id = #{id}
    </select>

    <select id="findByArticleId" resultMap="commentResultMap">
        SELECT * FROM comments
        WHERE article_id = #{articleId} AND parent_id IS NULL
        ORDER BY created_at ASC
    </select>

    <select id="findByParentId" resultMap="commentResultMap">
        SELECT * FROM comments
        WHERE parent_id = #{parentId}
        ORDER BY created_at ASC
    </select>

    <select id="findAll" resultMap="commentResultMap">
        SELECT * FROM comments
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM comments
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (article_id, parent_id, author, content, user_id, avatar_url, status, created_at)
        VALUES (#{articleId}, #{parentId}, #{author}, #{content}, #{userId}, #{avatarUrl}, 'APPROVED', NOW())
    </insert>

    <update id="updateStatus">
        UPDATE comments SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM comments WHERE id = #{id}
    </delete>

    <select id="countByArticleId" resultType="long">
        SELECT COUNT(*) FROM comments WHERE article_id = #{articleId}
    </select>

</mapper>